BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Pedido] ADD [andamento] NVARCHAR(1000) NOT NULL CONSTRAINT [Pedido_andamento_df] DEFAULT 'Solicitado';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
